## API Report File for "gngt-srcs"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { CollectionViewer } from '@angular/cdk/collections';
import { DataSource } from '@angular/cdk/collections';
import { MatPaginator } from '@angular/material/paginator';
import { MatSort } from '@angular/material/sort';
import { Model } from '@gngt/core/common';
import { ModelActionTypes } from '@gngt/core/model';
import { ModelListParams } from '@gngt/core/common';
import { ModelQueryParams } from '@gngt/core/common';
import { ModelService } from '@gngt/core/model';
import { Observable } from 'rxjs';
import { State } from '@gngt/core/model';

// @public (undocumented)
export class ModelDataSource<T extends Model, S extends State<T> = State<T>, A extends ModelActionTypes = ModelActionTypes, MS extends ModelService<T, S, A> = ModelService<T, S, A>> extends DataSource<T> {
    constructor(_service: MS, _baseParams?: ModelListParams);
    // (undocumented)
    get baseQueryParams(): Partial<ModelQueryParams> | null;
    set baseQueryParams(params: Partial<ModelQueryParams | null>);
    // (undocumented)
    connect(_: CollectionViewer): Observable<T[]>;
    // (undocumented)
    get data(): T[];
    // (undocumented)
    set dataModifier(dataModifier: (data: T[]) => Observable<T[]>);
    // (undocumented)
    disconnect(_: CollectionViewer): void;
    // (undocumented)
    get filter(): string;
    set filter(filter: string);
    // (undocumented)
    get filters(): ModelDataSourceFilters;
    set filters(filters: ModelDataSourceFilters);
    // (undocumented)
    get freeTextSearchFields(): string[];
    set freeTextSearchFields(freeTextSearchFields: string[]);
    // (undocumented)
    get paginator(): MatPaginator | null;
    set paginator(paginator: MatPaginator | null);
    // (undocumented)
    refresh(): void;
    // (undocumented)
    get sort(): MatSort | null;
    set sort(sort: MatSort | null);
}

// @public
export type ModelDataSourceFilters = {
    [key: string]: any;
};

// (No @packageDocumentation comment for this package)

```
