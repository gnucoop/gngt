package(default_visibility = ["//visibility:public"])

load("//tools:defaults.bzl", "ng_module")

ng_module(
    name = "examples-page",
    srcs = glob(["**/*.ts"]),
    deps = [
        "//src/dev-app-ion/example",
        "//src/ionic-examples",
        "@npm//@angular/router",
    ],
)
